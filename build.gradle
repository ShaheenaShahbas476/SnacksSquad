buildscript {
    ext {
        compose_ui_version = '1.2.0'
    }
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // Update to the latest stable version of the Android Gradle Plugin
        classpath 'com.android.tools.build:gradle:8.0.0'  // You can change to 8.0.1 if newer is available
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:1.9.0"  // Updated to a newer Kotlin version
    }
}


